{% extends 'backendbase.html.twig' %}

{% block title %}Produit
{% endblock %}

{% block body %}
	<h1>Produit</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ produit.id }}</td>
			</tr>
			<tr>
				<th>Nom</th>
				<td>{{ produit.nom }}</td>
			</tr>
			<tr>
				<th>Description</th>
				<td>{{ produit.description }}</td>
			</tr>
			<tr>
				<th>Taille</th>
				<td>{{ produit.taille }}</td>
			</tr>
			<tr>
				<th>Color</th>
				<td>{{ produit.color }}</td>
			</tr>
			<tr>
				<th>Image</th>
				<td>{{ produit.image }}</td>
			</tr>
			<tr>
				<th>Prix</th>
				<td>{{ produit.prix }}</td>
			</tr>
			<tr>
				<th>Qte_stock</th>
				<td>{{ produit.qteStock }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('produit_index') }}">back to list</a>

	<a href="{{ path('produit_edit', {'id': produit.id}) }}">edit</a>

	{{ include('produit_crud/_delete_form.html.twig') }}
{% endblock %}
